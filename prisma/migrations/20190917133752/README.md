# Migration `20190917133752`

This migration has been generated by Alex Erik at 9/17/2019, 1:37:52 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_File" (
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "encoding" TEXT NOT NULL DEFAULT ''  ,
  "filename" TEXT NOT NULL DEFAULT ''  ,
  "id" TEXT NOT NULL   ,
  "mimetype" TEXT NOT NULL DEFAULT ''  ,
  "path" TEXT NOT NULL DEFAULT ''  ,
  "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  PRIMARY KEY ("id")
);

INSERT INTO "new_File" ("encoding","filename","id","mimetype","path") SELECT "encoding","filename","id","mimetype","path" from "File"

DROP TABLE "lift"."File";

ALTER TABLE "lift"."new_File" RENAME TO "File";

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190917122625-files..20190917133752
--- datamodel.dml
+++ datamodel.dml
@@ -30,9 +30,16 @@
 }
 model File {
   id       String @default(cuid()) @id
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
   path     String
   filename String
   mimetype String
   encoding String
+}
+
+enum Role {
+  USER
+  ADMIN
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20190917133752)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190917133752'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
