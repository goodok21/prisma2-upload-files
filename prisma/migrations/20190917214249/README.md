# Migration `20190917214249`

This migration has been generated by Alex Erik at 9/17/2019, 9:42:49 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_File" (
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "description" TEXT NOT NULL DEFAULT ''  ,
  "id" TEXT NOT NULL   ,
  "mimetype" TEXT NOT NULL DEFAULT ''  ,
  "path" TEXT NOT NULL DEFAULT ''  ,
  "size" INTEGER NOT NULL DEFAULT 0  ,
  "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  PRIMARY KEY ("id")
);

INSERT INTO "new_File" ("createdAt","id","mimetype","path","size","updatedAt") SELECT "createdAt","id","mimetype","path","size","updatedAt" from "File"

DROP TABLE "lift"."File";

ALTER TABLE "lift"."new_File" RENAME TO "File";

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190917181534..20190917214249
--- datamodel.dml
+++ datamodel.dml
@@ -35,8 +35,9 @@
   updatedAt DateTime @updatedAt
   path     String
   size     Int
   mimetype String
+  description String
 }
 enum Role {
   USER
```

## Photon Usage

You can use a specific Photon built for this migration (20190917214249)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190917214249'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
