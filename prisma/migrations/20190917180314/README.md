# Migration `20190917180314`

This migration has been generated by Alex Erik at 9/17/2019, 6:03:14 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_File" (
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "encoding" TEXT    ,
  "filename" TEXT    ,
  "id" TEXT NOT NULL   ,
  "mimetype" TEXT    ,
  "path" TEXT NOT NULL DEFAULT ''  ,
  "size" INTEGER    ,
  "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  PRIMARY KEY ("id")
);

INSERT INTO "new_File" ("createdAt","encoding","filename","id","mimetype","path","size","updatedAt") SELECT "createdAt","encoding","filename","id","mimetype","path","size","updatedAt" from "File"

DROP TABLE "lift"."File";

ALTER TABLE "lift"."new_File" RENAME TO "File";

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190917165011..20190917180314
--- datamodel.dml
+++ datamodel.dml
@@ -33,12 +33,12 @@
   id       String @default(cuid()) @id
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
   path     String
-  size     Int
-  filename String
-  mimetype String
-  encoding String
+  size     Int?
+  filename String?
+  mimetype String?
+  encoding String?
 }
 enum Role {
   USER
```

## Photon Usage

You can use a specific Photon built for this migration (20190917180314)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190917180314'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
